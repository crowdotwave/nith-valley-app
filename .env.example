# Copy to .env and fill in from the Supabase dashboard (Project Settings > API).
# Both of these are safe to ship in a client bundle. Row level security is
# what protects the data, not the key.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
